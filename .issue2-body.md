## 现象

设置面板（侧边栏底部 设置 → 模型/插件 导航）中，鼠标悬停到文字/行上时文字变成与悬停底色几乎同色，几乎看不见。HARNESS 徽标修复后仍存在此问题。

## 根因（同 HARNESS 一类病根：调色板漏盖明暗随变的 DSH 变量）

设置面板导航行（SettingsRoot）样式：

- `.navCell` 文字色 = `--dsw-alias-label-primary`（调色板设为主题 text，opencode dark 下 #EEEEEE）；
- `.navCell:hover` 背景 = `--dsw-specific-sidebar-nav-item-hover`（**调色板未覆盖**）。

该变量在 DSH 里随明暗取两套默认值：light = `neutral-bluish-75` ≈ #F1F3F5（近白）/ dark = `neutral-bluish-850` ≈ #2C2C2E。主题偏好默认 `system`：当系统/应用处于浅色（无 `data-ds-dark-theme`）时，调色板把界面整体刷成深色、文字刷成 #EEEEEE，而 hover 背景却取浅色默认 #F1F3F5 → 白字撞浅底，不可见。

## 修复

`src/engine/map-dsh.mjs` 补上三个导航项变量的主题派生覆盖（alpha 叠加，与现有 `interactive-bg-*` 同一约定，明暗无关、不再随 DSH 默认漂移）：

- `--dsw-specific-sidebar-nav-item-hover` → `withAlpha(text, 0.08)`
- `--dsw-specific-sidebar-nav-item-active` → `withAlpha(text, 0.14)`
- `--dsw-specific-sidebar-nav-item-active-accent` → `withAlpha(primary, 0.35)`

另做全量差集审计（DSH light/dark 两套 `--dsw-*` 取值），确认 17 个明暗随变且调色板未覆盖的变量中，其余为背景蒙层/边框/状态色，无同类文字对比度风险。
