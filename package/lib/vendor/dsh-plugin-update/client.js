// 派生文件（构建生成，人手不改）：由 npm 包 dsh-plugin-update@0.1.1 的 dist/ 原样复制。
// 为什么必须放进本包：更新包的读取器用 containingPackage(import.meta.url, 目标包名) 从自己的文件位置
// 往上找目标包；以 npm 依赖形态安装时它住在别的目录，会抛 unknown-profile 且环境判定恒为 installation-changed。
// 门禁：tests/update-panel.test.mjs 断言本副本与 npm 包逐字节一致（除本头部）。
// vendor-source: dsh-plugin-update@0.1.1 dist/client.js
// AUTO-GENERATED by node packages/dsh-plugin-update/build.mjs — DO NOT EDIT. Source: packages/dsh-plugin-update/src/client.ts
import { buildPhoneNames, DEFAULT_PANEL_POLL_MS, MIN_PANEL_POLL_MS } from "./config.js";
import { buildPhoneName, buildPhoneNames as buildPhoneNames2 } from "./config.js";
import { manualCommand as manualCommand2 } from "./commands.js";
const CLIENT_POLL = {
  defaultMs: DEFAULT_PANEL_POLL_MS,
  minMs: MIN_PANEL_POLL_MS
};
function buildClientPhoneNames(prefix) {
  return buildPhoneNames(prefix);
}
function assertPollInterval(ms) {
  if (typeof ms !== "number" || !Number.isFinite(ms) || ms < MIN_PANEL_POLL_MS) {
    throw new Error("[dsh-plugin-update] \u9762\u677F\u8F6E\u8BE2\u95F4\u9694\u975E\u6CD5\uFF1A\u4E0D\u5F97\u5C0F\u4E8E 250 \u6BEB\u79D2\uFF08\u6536\u5230 " + JSON.stringify(ms) + "\uFF09");
  }
  return ms;
}
export {
  CLIENT_POLL,
  assertPollInterval,
  buildClientPhoneNames,
  buildPhoneName,
  buildPhoneNames2 as buildPhoneNames,
  manualCommand2 as manualCommand
};
